import { ScalingTextDirective } from './scaling-text.directive';

describe('ScalingTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ScalingTextDirective();
    expect(directive).toBeTruthy();
  });
});
